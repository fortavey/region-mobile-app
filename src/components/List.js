import React from "react";
import { View, Text, StyleSheet } from "react-native"; 

export const List = (props) => {
    return (
        <View style={styles.list}>
            <Text>Список регионов</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    list: {

    },
})
